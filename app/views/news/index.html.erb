<ul class="divided">
  <% @news.each do |novelty| %>
    <li>
      <article class="box post-summary">
        <h3><%= link_to novelty.title, news_path(novelty) %></h3>
        <%= (image_tag novelty.image.url, :class => "news_image_mini", alt: novelty.title) if novelty.image_stored? %>
        <p>
          <%= novelty.description.split[0..32].join(' ') %>...
        </p>
        <ul class="meta">
          <li class="icon fa-clock-o"><%= time_ago_in_words(novelty.created_at) %></li>
        </ul>
      </article>
      <a href=<%= news_path novelty %> class="button alt">Подробнее</a>
    </li>
  <% end %>
</ul>
